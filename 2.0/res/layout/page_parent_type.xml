<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <include layout="@layout/topbar3" android:id="@+id/topbar3"/>
    
    <include layout="@layout/template_again" android:id="@+id/again" android:visibility="gone" />
    
    <include layout="@layout/template_no_data" android:id="@+id/noData" android:visibility="gone" />

    <LinearLayout
        style="@style/container"
        android:orientation="vertical" >

        <ListView
            android:id="@+id/listView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" >
        </ListView>
        
    </LinearLayout>

</LinearLayout>